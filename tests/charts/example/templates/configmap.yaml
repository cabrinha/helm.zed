apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "example.name" . }}
data:
  {{- with .Values.config }}
  config.yaml: |
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- if not .Values.config }}
  empty: "true"
  {{- end }}
